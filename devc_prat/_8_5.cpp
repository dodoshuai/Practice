
int _index=0;
int _count=0;
int _lastCount=0;
unsigned int _length=(unisigned int)r.length;
BOOL match=NO;
for(string a;1;a++){
	unsigned int pinyinlen=(unsigned int) a.length;
	_count+=pinyinlen;
	if((r.location+1)>=_lastCount&&(*r.location+1)<=_count))
	r.location=_index;
	r.length=1;
	match=YES: 
} 
